# 原子游创宣传片项目交接说明

更新时间：2026-08-11

项目根目录：`E:\原子游创宣传片`

主工程目录：`E:\原子游创宣传片\promo-video`

当前主成片：`E:\原子游创宣传片\promo-video\out\原子游创宣传片-最终版.mp4`

---

## 0. 一分钟交接摘要

这是一个使用 **Remotion + React + TypeScript** 制作的程序化宣传片工程，目标是在展厅中循环展示「原子游创」品牌及其产品体系。

当前成片的核心叙事是：

`原子游创平台定位 → Dora SSR 多语言编码智能体 → Dora 生成可运行游戏 → DSGO 发现/游玩/创作/交流 → 赛事与开放生态 → 原子游创品牌收束`

产品关系必须准确理解：

- **原子游创**：主品牌，定位为 AI 开源游戏共创平台。
- **Dora SSR**：多语言编码智能体与游戏创作引擎，负责理解需求、协助编码、调试并生成可运行结果。
- **DSGO**：面向玩家和创作者的产品入口，支持发现、详情浏览、游玩、讨论和发起创作。
- DSGO 的创作能力由 Dora SSR 驱动，但 DSGO 本身也承担发现、游玩、社区和创作功能，不能被描述成纯展示页。

当前最终版已经完成主要视觉和逻辑验收。后续如果只是做小改动，应从现有源码继续，不要恢复旧版顺序、旧素材或已经淘汰的转场。

---

## 1. 项目目标与使用场景

### 1.1 当前主要用途

- 展厅大屏循环播放。
- 工作汇报、产品介绍和合作交流。
- 后续可从当前工程派生 Bilibili 宣传版、短版预告和功能演示片。

### 1.2 当前传播目标

观众应当在观看后理解以下事实：

1. 原子游创连接游戏、AI、开源、创作者、玩家与合作生态。
2. Dora SSR 不是单纯的 Logo，而是能够执行创作任务的多语言编码智能体。
3. Dora SSR 可以把自然语言需求推进到代码和可运行游戏结果。
4. DSGO 是游戏被发现、游玩、讨论和继续创作的产品入口。
5. 平台通过社区、赛事、奖项和合作机构形成开放创作生态。

### 1.3 不应出现的错误表述

- 不要把 Dora SSR 和 DSGO 当成同一个产品。
- 不要把 DSGO 描述成只能浏览或只能玩游戏的平台。
- 不要把 Dora SSR 放到 DSGO 之后再解释其创作能力。
- 不要声称“完全零门槛”“自动完成所有游戏开发”等未经产品验证的能力。
- 不要用虚构操作界面替代已有真实录屏，也不要伪造尚不存在的完整业务流程。

---

## 2. 当前交付成果

### 2.1 最终版成片

| 项目 | 当前值 |
| --- | --- |
| 文件 | `promo-video/out/原子游创宣传片-最终版.mp4` |
| 时长 | 104.576 秒 |
| 总帧数 | 3136 帧 |
| 帧率 | 30fps |
| 分辨率 | 1920x1080 |
| 画幅 | 16:9 |
| 视频编码 | H.264 High |
| 像素格式 | `yuvj420p` |
| 音频编码 | AAC LC |
| 音频规格 | 48kHz、双声道 |
| 文件大小 | 36,649,747 字节，约 36.65MB |
| 平均码率 | 约 2.80Mbps |
| SHA-256 | `08EE5FBCCFECCCB711F8D30E66BCDBA3BFBDA73EB66FCD90B08D2CE277E98AD2` |

该文件由之前验收通过的 `原子游创宣传片-修改版候选-v3.mp4` 改名而来，文件内容和哈希没有变化。

### 2.2 仍保留的辅助输出

`promo-video/out/` 中还保留以下本地参考文件：

- `原子游创宣传片-修改版候选-v2.mp4`：最终生态转场修正前的候选版，仅用于对比。
- `DSGO-Dora功能段-最终候选.mp4`：Dora 与 DSGO 独立功能段候选。
- `掌机宣传片段.mp4`：掌机展示段。
- `掌机完整游玩.mp4`：掌机完整玩法预览。
- `掌机屏幕预览.mp4`：掌机屏幕快速预览。

除非需要做回溯比较，后续不应从这些辅助文件反向替换当前主时间线。

### 2.3 输出文件的 Git 策略

以下内容已通过根目录 `.gitignore` 排除：

- `**/out/qa/`
- `**/out/review*/`
- `**/out/*候选*.mp4`
- `**/out/*最终版*.mp4`
- `**/out/*.tmp`

因此最终 MP4 默认只保留在本机，不上传到 Git 提交历史。需要在线分发时，建议作为 GitHub Release 附件、网盘文件或正式媒体资产发布，不要直接提交到源码目录。

---

## 3. GitHub 与版本控制状态

### 3.1 远程仓库

- 仓库：`https://github.com/TangAnNing/atom-game-promo`
- 可见性：**PUBLIC（公开）**
- 默认分支：`main`
- 当前远程提交：`e47a355 Update product overview and promo documentation`

### 3.2 Git LFS

根目录 `.gitattributes` 当前配置：

```gitattributes
*.mp4 filter=lfs diff=lfs merge=lfs -text
*.pptx filter=lfs diff=lfs merge=lfs -text
```

MP4 和 PPTX 使用 Git LFS。新环境克隆项目后必须安装 Git LFS 并执行：

```powershell
git lfs install
git lfs pull
```

如果素材文件打开后只有三行文本，内容类似 `version https://git-lfs.github.com/spec/v1`，说明只下载了 LFS 指针，没有下载真实文件。

### 3.3 当前本地未提交内容

截至本交接文档更新时，本地工作区包含以下文档类修改：

- `.gitignore`：新增最终版 MP4 忽略规则。
- `README.md`：更新本地最终版文件名说明。
- `项目交接说明.md`：本次完整重写。

提交前先运行：

```powershell
git status --short
git diff --check
git diff
```

确认只包含预期文档后再提交。不要用 `git reset --hard` 或 `git checkout --` 清理工作区，因为可能覆盖其他人的本地修改。

### 3.4 公开仓库风险

当前仓库是公开仓库，仓库历史中包含：

- `原子游创商业计划书.pptx`（通过 Git LFS 管理）。
- `images/` 下的原始图片、录屏和视频素材。
- `promo-video/public/media/` 下的产品录屏与合作机构素材。

必须由项目负责人确认这些内容是否确实允许公开。如果商业计划书、测试账号信息、内部路径或原始素材不应公开，仅在后续提交中删除文件是不够的，因为 Git 历史和 LFS 对象可能仍可访问，需要另行进行历史清理和 LFS 管理。

---

## 4. 技术栈与验证环境

### 4.1 核心技术

- Remotion 4.0.499
- React 19.2.7
- React DOM 19.2.7
- TypeScript 6.0.3
- `@remotion/media` 4.0.499
- `@remotion/layout-utils` 4.0.499
- FFmpeg / ffprobe
- Git LFS

### 4.2 当前验证过的本机环境

| 工具 | 版本 |
| --- | --- |
| Node.js | v22.19.0 |
| npm | 10.9.3 |
| FFmpeg | 8.1.2 full build |
| Git LFS | 3.7.1 |
| 操作系统 | Windows / PowerShell |

不要求后续环境版本完全相同，但建议使用 Node.js 20 或更高版本，并以 `package-lock.json` 锁定依赖。

### 4.3 安装

在 `E:\原子游创宣传片\promo-video` 中运行：

```powershell
npm install
```

如果只需要按锁文件严格安装，也可使用：

```powershell
npm ci
```

---

## 5. Remotion 工程入口

调用关系如下：

```text
src/index.ts
  -> src/Root.tsx
      -> Composition: AtomGamePromo
          -> src/promo/AtomGamePromo.tsx
              -> src/promo/timeline.ts
              -> 各场景组件
              -> src/promo/audio.tsx
```

### 5.1 当前 Composition

| Composition ID | 时长 | 用途 |
| --- | ---: | --- |
| `AtomGamePromo` | 3136 帧 / 104.53 秒 | 正式主宣传片 |
| `HandheldScreenPreview` | 210 帧 / 7 秒 | 掌机屏幕快速预览 |
| `HandheldFullGameplay` | 430 帧 / 14.33 秒 | 掌机完整游玩预览 |
| `HandheldPromoInsert` | 339 帧 / 11.30 秒 | 可独立插入的掌机宣传段 |
| `BrandMorphOutroPreview` | 210 帧 / 7 秒 | 品牌收尾单独预览 |

查看 Composition：

```powershell
npx remotion compositions src/index.ts
```

### 5.2 `.tsx` 文件的含义

本项目中的 `.tsx` 是 React + TypeScript 组件文件，用代码描述画面内容和运动：

- 文案、图片、视频和 Logo。
- 元素位置、尺寸、透明度和层级。
- 缩放、移动、遮罩、缓动和转场。
- 每个镜头如何根据当前帧变化。

`.ts` 文件主要负责时间线、主题、数据和辅助逻辑；`.tsx` 文件主要负责可渲染画面。

---

## 6. 目录结构与职责

```text
E:\原子游创宣传片
├─ README.md                       GitHub 首页说明
├─ 项目交接说明.md                本文档
├─ 原子游创商业计划书.pptx         项目背景资料，已进入公开 Git/LFS 历史
├─ images/                         原始及补充素材
├─ styleframes/                    前期视觉样稿
└─ promo-video/
   ├─ package.json                 工程依赖与 npm 脚本
   ├─ src/index.ts                 Remotion 注册入口
   ├─ src/Root.tsx                 Composition 注册
   ├─ src/promo/                   主宣传片
   ├─ src/dsgo-segment/            DSGO 与 Dora 功能段
   ├─ public/media/                正式渲染所用媒体
   ├─ public/audio/                BGM、SFX 与授权记录
   └─ out/                         本地渲染和 QA 输出，默认不提交
```

### 6.1 主宣传片关键文件

| 文件 | 作用 |
| --- | --- |
| `src/promo/AtomGamePromo.tsx` | 正式场景顺序和 Sequence 组装 |
| `src/promo/timeline.ts` | 所有正式镜头的起始帧与时长 |
| `src/promo/audio.tsx` | BGM 和全片 SFX 钉帧表 |
| `src/promo/theme.ts` | 色板与字体 |
| `src/promo/motion.ts` | 通用运动和入场辅助 |
| `src/promo/BrandMorphOutroPreview.tsx` | 当前正式使用的品牌收尾 |

### 6.2 场景文件

| 文件 | 对应内容 |
| --- | --- |
| `scenes/BrandOpen.tsx` | 原子游创品牌开场 |
| `scenes/Positioning.tsx` | 游戏、AI、开源定位 |
| `scenes/DoraCore.tsx` | Dora SSR Logo 和核心定位 |
| `scenes/DoraCapabilities.tsx` | 多语言能力与编码智能体 |
| `../dsgo-segment/scenes.tsx` | Dora 实际创作、结果、DSGO 品牌、发现、详情、游玩、社区 |
| `scenes/HandheldOnly.tsx` | 掌机展示 |
| `scenes/CreateEntry.tsx` | DSGO 创作入口 |
| `scenes/Events.tsx` | 赛事三卡与作品瀑布墙 |
| `scenes/Ecosystem.tsx` | 奖项、承办/协办单位与合作伙伴 |

### 6.3 可复用组件

| 文件 | 作用 |
| --- | --- |
| `components/SceneBase.tsx` | 深色网格背景、基础标题等 |
| `components/PhoneShell.tsx` | 手机外壳和屏幕承载 |
| `components/HandheldStudioPlayer.tsx` | 掌机舞台和屏幕播放 |
| `components/MirroredEventShowcase.tsx` | 赛事三卡镜面结构 |
| `components/PageWaterfallWall.tsx` | 作品瀑布墙场景封装 |
| `components/VerticalTicker.tsx` | Video Shotcraft 三列无限滚动组件 |

---

## 7. 当前正式时间线

时间线定义：`promo-video/src/promo/timeline.ts`

帧率：30fps

总帧数：3136

| # | Shot Key | 帧范围 | 时长 | 时间范围 | 场景组件 | 主要信息 |
| ---: | --- | ---: | ---: | ---: | --- | --- |
| 1 | `brand` | 0-182 | 183f | 0.00-6.10s | `BrandOpen` | 原子游创品牌开场 |
| 2 | `positioning` | 183-329 | 147f | 6.10-11.00s | `Positioning` | 游戏、AI、开源平台定位 |
| 3 | `dora` | 330-504 | 175f | 11.00-16.83s | `DoraCore` | Dora SSR Logo 与创作引擎身份 |
| 4 | `doraLanguages` | 505-651 | 147f | 16.83-21.73s | `DoraLanguages` | 多语言支持 |
| 5 | `doraAgent` | 652-798 | 147f | 21.73-26.63s | `DoraAgent` | 理解、编写、调试等编码智能体能力 |
| 6 | `doraBuild` | 799-1034 | 236f | 26.63-34.50s | `DoraBuildScene` | Dora 真实创作工作区 |
| 7 | `doraResult` | 1035-1152 | 118f | 34.50-38.43s | `DoraResultScene` | 可运行的游戏结果 |
| 8 | `dsgoBrand` | 1153-1270 | 118f | 38.43-42.37s | `DsgoBrandScene` | DSGO Logo 与 Dora 驱动关系 |
| 9 | `discover` | 1271-1447 | 177f | 42.37-48.27s | `DiscoverScene` | 像刷视频一样发现游戏 |
| 10 | `detail` | 1448-1565 | 118f | 48.27-52.20s | `DetailScene` | 右滑展开作品世界 |
| 11 | `play` | 1566-1801 | 236f | 52.20-60.07s | `PlayScene` | 无需下载、直接游玩 |
| 12 | `handheld` | 1802-2008 | 207f | 60.07-66.97s | `HandheldOnly` | 掌机也能玩 |
| 13 | `create` | 2009-2185 | 177f | 66.97-72.87s | `CreateEntry` | 在 DSGO 发起创作 |
| 14 | `community` | 2186-2362 | 177f | 72.87-78.77s | `CommunityScene` | 论坛和作品讨论空间 |
| 15 | `events` | 2363-2630 | 268f | 78.77-87.70s | `Events` | 赛事连接创作者与作品瀑布墙 |
| 16 | `ecosystem` | 2631-2925 | 295f | 87.70-97.53s | `Ecosystem` | 奖项、机构、伙伴与开放生态 |
| 17 | `outro` | 2926-3135 | 210f | 97.53-104.53s | `BrandMorphOutroPreview` | 游戏结果收束为原子游创品牌 |

时间线边界已经按约 122 BPM 的 BGM 拍点微调，最大调整不超过 9 帧。修改一个镜头时长会影响后续全部镜头和 SFX，不要只移动画面而忘记重对声音。

---

## 8. 分段叙事与已确认逻辑

### 8.1 品牌开场：0.00-11.00s

目标：先建立原子游创品牌，再说明游戏、AI、开源三种能力在平台汇合。

已确认：

- 主品牌必须在第一屏清楚出现。
- 开场保持单一视觉主角，不堆叠产品 Logo。
- 重要字标落定后保留呼吸时间。

### 8.2 Dora SSR 能力：11.00-26.63s

顺序必须是：

`Dora SSR Logo → 选择项目合适语言 → 编码智能体能力`

原因：先说明产品身份，再展示其能力，避免语言图标先出现但观众不知道属于谁。

已确认：

- Dora SSR Logo 位于多语言场景之前。
- 多语言图标使用透明素材。
- Logo 入场和多语言入场使用不同 SFX，避免连续重复同一种声音。
- Dora SSR 文案必须包含“多语言编码智能体”与创作引擎定位。

### 8.3 Dora 真实创作与结果：26.63-38.43s

目标：用真实工作区证明 Dora SSR 不只是概念动画。

录屏处理原则：

- 原 HEVC 录屏裁掉浏览器标签栏和地址栏。
- 裁切从原素材 `y=114` 开始。
- 横向完整保留目录、Dora 标签、正文、输入区和工具栏。
- 顶部不保留宽白条。
- 工作素材仅在底部补约 34px 深色窄边适配 1920x1080。
- 不恢复左上角多余线条。

创作过程之后必须展示真实可运行游戏结果，完成“需求 → 编码 → 结果”的证据链。

### 8.4 Dora 结果到 DSGO：38.43-42.37s

这是全片最重要的产品关系转场之一：

- Dora 生成的可玩结果收束成 DSGO 图标。
- DSGO Logo 随后完整出现。
- 观众应理解为“Dora SSR 驱动创作，DSGO 承载用户体验”。

不能把 DSGO Logo 移到 Dora 能力之前，也不能删掉 Dora 结果到 DSGO 的逻辑桥梁。

### 8.5 DSGO 用户体验：42.37-66.97s

顺序是：

`发现 → 详情 → 游玩 → 掌机`

已确认：

- 使用新的 DSGO 手机录屏，而不是旧版简陋示例。
- 手机发现、详情和游玩尽量保持同一设备外壳连续，避免每段重新出现手机。
- 详情段不保留多余的右滑箭头动画。
- 游玩段强调无需下载、即刻开玩。
- 掌机部分保留，不删除；它用于表达 DSGO 内容可以延展到不同游玩场景。

### 8.6 创作与社区：66.97-78.77s

目标：说明 DSGO 用户不只是玩家，也可以发起创作并进入作品讨论空间。

已确认：

- 手机打字和创作过程不使用早期过于简单的打字机模拟。
- 创作入口与真实产品页面保持一致。
- 社区段说明作品有独立讨论空间和持续反馈。

### 8.7 赛事与作品瀑布墙：78.77-87.70s

赛事段包含两个阶段：

1. 三卡镜面展示。
2. 作品瀑布墙。

#### 三卡镜面展示不可回退项

- 中间主卡：`大赛介绍.png`，正视、最大、前景。
- 左侧卡：`赛事日历.png`，向内倾斜并后退。
- 右侧卡：`赛事奖励.png`，反向等角度倾斜并后退。
- 左右等距、对称并适度压暗。
- 不改回普通三图平铺。

#### 瀑布墙不可回退项

- 使用 Video Shotcraft 的 `VerticalTicker` 运动逻辑。
- 三列方向：左向上、中间向下、右向上。
- 周期：12 秒、9 秒、14 秒。
- 保留 `perspective: 1000px`、`rotateX(20deg)`、`scale(1.2)`。
- 列宽 560，间距 30。
- 保留无限循环、上下暗角、圆角卡片和斜视透视。
- 左右列使用“展示”素材，中间列使用“陈列”素材。
- 当前主片瀑布墙实际展示约 5 秒，不再恢复早期约 10 秒的长度。

文案：

- 标题：`更多作品持续涌现`
- 副标题：`从赛事展示，走向持续交流与连接`

### 8.8 奖项、机构和伙伴：87.70-97.53s

必须依次展示：

1. 奖项：`让好作品 / 走得更远`
2. 承办与协办单位：`汇聚多方力量 / 共创赛事舞台`
3. 合作伙伴：`与伙伴共建 / 开放创作生态`

已确认：

- 三个阶段先退后进，不能出现同坐标文字双曝光。
- 阶段边界保留深色网格和光场，不产生持续黑场。
- 合作机构使用透明底素材。
- 机构名称位于图标正下方。
- AtomGit 使用新裁剪素材，视觉权重与卡赢股份接近。
- 当前三家：龙湾开源信创技术研究院、浙江卡赢科技股份有限公司、AtomGit。

### 8.9 品牌收束：97.53-104.53s

当前使用 `BrandMorphOutroPreview`：

- 游戏结果先成为主角。
- 游戏画面收束为原子游创品牌标志。
- Logo 落定后保留稳定停留。
- 主片中该组件设置 `withAudio={false}`，避免与全片 `PromoAudio` 重复叠音。

当前首尾不是完全无缝循环：结尾 Logo 后回到开头近黑背景，约 0.23 秒暗场。它不属于损坏，但如果展厅要求严格无缝循环，应专门制作首尾闭环版本。

---

## 9. 视觉系统

### 9.1 色板

定义于 `src/promo/theme.ts`：

| Token | 色值 | 用途 |
| --- | --- | --- |
| `ink` | `#151817` | 主背景 |
| `inkSoft` | `#202423` | 次级深色表面 |
| `paper` | `#f4f1e8` | 暖白辅助色 |
| `white` | `#fffdf6` | 主文字 |
| `yellow` | `#f5bb19` | 原子游创、DSGO、游戏能力 |
| `yellowDeep` | `#d99708` | 深黄色强调 |
| `cyan` | `#55d8d0` | Dora SSR、AI、编码能力 |
| `coral` | `#ff6f73` | 赛事、合作和生态 |
| `magenta` | `#e9479a` | 补充强调 |

### 9.2 字体

- 主字体：`Microsoft YaHei UI` / `Microsoft YaHei`
- 等宽字体：`Consolas` / `Microsoft YaHei UI`

在非 Windows 环境渲染时需要检查字体替换，否则中文宽度、换行和对齐可能发生变化。

### 9.3 统一视觉语言

- 深色 58px 网格背景。
- 黄色、青色、珊瑚红分别编码不同功能章节。
- 重要信息使用较大标题；真实 UI 保持正视和清晰。
- 动效主要依赖位移、缩放、遮罩、透明度、缓动和少量光效。
- 产品宣传片默认不加入手持抖动。
- 不群发廉价扫光；光效只服务主要元素。

### 9.4 当前展厅可读性风险

对最终版每秒抽样 105 帧测得平均亮度约 `41.2/255`，整体属于暗色设计。源码中仍存在部分 17-27px、低透明度说明文字。

在明亮展厅、55-75 英寸屏幕、观看距离 2-5 米时：

- 主标题可以阅读。
- 小字说明和真实 UI 内部文字可能难以阅读。
- 暗部工作区可能受到环境反光影响。

如果现场测试发现问题，优先提高次要文字到约 30-36px、增加文字不透明度和整体中间调，不要先增加更多装饰动画。

---

## 10. 关键素材

### 10.1 Dora 与 DSGO

| 素材 | 路径 | 说明 |
| --- | --- | --- |
| Dora SSR Logo | `public/media/dorassr-logo.png` | Dora 品牌镜头 |
| Dora 创作录屏 | `public/media/dsgo-segment/dora-create-1080p.mp4` | 已裁掉浏览器外壳的正式录屏 |
| DSGO 图标 | `public/media/dsgo-segment/dsgo-icon.jpg` | DSGO 品牌段 |
| DSGO 手机流 | `public/media/dsgo-segment/phone-feed.mp4` | 发现与产品入口素材 |
| 连续刷游戏 | `public/media/首页连续刷游戏-流畅1.2x.mp4` | 主片手机发现段 |
| 点击进入游戏 | `public/media/刷游戏点击进入游戏视频.mp4` | 详情与游玩衔接 |
| 手机游戏精选 | `public/media/游戏画面2-手机精选.mp4` | 手机内游戏画面 |

### 10.2 多语言图标

目录：`public/media/languages/`

- Lua
- TypeScript
- Teal
- YueScript
- Rust
- C#
- wa

目录内同时存在 `C#.svg` 和 `csharp.svg`，后续清理前先确认源码实际引用，避免误删。

### 10.3 掌机素材

`public/media/` 中包含多版掌机抠图和工作文件。当前场景依赖正式版本，不要根据文件名简单删除旧版；删除前用 `rg` 搜索引用。

### 10.4 赛事与作品

- `大赛介绍.png`
- `赛事日历.png`
- `赛事奖励.png`
- `展示1.jpg` 至 `展示12.jpg`
- `陈列1.png` 至 `陈列7.png`
- `作品展示1.png` 至 `作品展示3.png`

### 10.5 生态与合作机构

- `总体奖项.png`
- `承办单位协办单位.png`
- `public/media/partners/longwan.png`
- `public/media/partners/card-winner.png`
- `public/media/partners/AtomGit.png`

合作机构目录同时保留中文文件名和标准化英文文件名，当前 `Ecosystem.tsx` 使用英文文件名版本。

### 10.6 素材处理原则

- 正式渲染只读取 `promo-video/public/` 下的文件。
- `images/` 是原始素材库，不应在场景中直接使用绝对路径。
- 新素材先复制/转码到 `public/media/`，再通过 `staticFile()` 引用。
- 视频尽量统一为 H.264、30fps；图片使用 PNG/JPG/SVG 并确认透明通道。
- 任何裁切都必须检查手机状态栏、底部导航、应用边缘和文字是否完整。

---

## 11. 声音设计

### 11.1 BGM

- 文件：`public/audio/house-vibez.mp3`
- 曲名：House Vibez
- 艺术家：Lily J
- 来源：Mixkit
- 记录 BPM：约 122-123
- 源码裁切：开头跳过 11 帧
- 主音量：0.34
- 全片前 30 帧淡入，最后 50 帧淡出

### 11.2 SFX 结构

`src/promo/audio.tsx` 中集中维护声明式钉帧表。所有声音位置使用：

```ts
SHOTS.scene.from + offset
```

不要改成裸帧号。这样移动某个章节时，该章节音效可以随场景起点移动。

主要声音语法：

- 运镜：`whoosh-fast` / `whoosh-big`
- 轻转场：`transition-soft` / `transition-snap`
- 落地：`impact-cine`
- 光效：`sparkle`
- 铺垫：`riser-cine`
- 键盘动作：`keyboard`
- 多语言图标：`swoosh-quick` + `pop`

### 11.3 当前响度

最终版 FFmpeg `volumedetect` 结果：

- 平均音量：约 `-21.4 dB`
- 峰值：约 `-5.3 dB`

该响度适合展厅背景播放，上传 Bilibili 前建议重新测量 LUFS，并根据平台听感制作单独上传母版。

### 11.4 音频授权风险

授权总表：`public/audio/ATTRIBUTION.md`

当前需要特别注意：

- `pop.mp3` 标记为“来源待考，商用前须确认”，并且主片多语言段正在实际使用。
- `typewriter.mp3` 同样来源待考，但当前主片 `PromoAudio` 未使用。
- 首批 Mixkit SFX 虽记录为 Mixkit License，但缺少逐文件 URL，商用前建议重新回查或替换为来源明确的扩充音效。

在公开展厅、Bilibili、广告投放或商业发布前，必须优先解决 `pop.mp3` 授权问题。

---

## 12. 已完成的关键修改

以下内容已经在当前最终版中完成，不应误以为仍待实现：

1. Dora SSR Logo 已移动到多语言能力之前。
2. 多语言段音效已与 Logo 段区分。
3. 瀑布墙已缩短为约 5 秒。
4. 重复的“让好的游戏走得更远”文案已经去重。
5. AtomGit 使用重新裁剪的透明素材，名称位于图标下方。
6. 合作机构已经改为透明图标和三列排布。
7. Dora 录屏顶部宽白条已经裁掉。
8. DSGO 左上角多余线条已经去掉。
9. “右滑，展开作品世界”的额外箭头已经去掉。
10. 掌机内容保留在正式主片中。
11. DSGO 已替换为新的手机录屏和产品素材。
12. 编码智能体面板可以连续扩展成真实 Dora 工作区。
13. Dora 游戏结果可以收束成 DSGO 图标。
14. DSGO 详情到游玩保持手机外壳连续。
15. 瀑布墙到生态段已经消除持续黑场。
16. 奖项、承办/协办单位、合作伙伴之间已经消除叠字。
17. Dora SSR Logo 的一次网页位置偏移已经撤销，当前源码恢复为最终版原始位置。

---

## 13. 当前 QA 结果

### 13.1 技术检查

- `npx tsc --noEmit`：通过。
- Remotion 打包与 Composition 读取：通过。
- 3136 帧 CPU 全片渲染：通过。
- H.264 视频流与 AAC 音频流：存在且可读取。
- 1920x1080、30fps：正确。
- 时长：104.576 秒。
- 音量：平均 -21.4dB，峰值 -5.3dB。

### 13.2 黑场与静止段

自动黑场检测在早期深色设计中识别到：

- 0.00-0.23s
- 6.10-6.27s
- 10.93-11.60s

这些位置对应开场和章节深色停顿，不是文件损坏。赛事瀑布墙和生态段修复后未出现异常持续黑场。

自动冻结检测识别出的 1-3 秒静止段，主要是品牌、Logo、标题和合作伙伴的有意停留，不是编码卡死。

### 13.3 独立视觉复核结论

此前独立审查未发现 P0/P1/P2 阻断问题，确认：

- 产品顺序正确。
- 关键转场无叠字或产品逻辑断裂。
- Dora 工作区没有顶部宽白条。
- 手机和掌机没有关键 UI 裁切。
- 三家合作机构同级关系成立。
- 结尾 Logo 完整稳定。

最后一轮修复后的生态段通过局部尾段重渲和全片抽帧复核。

---

## 14. 本地预览与编辑

### 14.1 启动网页预览

在 `promo-video` 目录运行：

```powershell
npm run dev
```

Remotion Studio 通常会在类似以下地址启动：

```text
http://localhost:3000
```

如果端口被占用，Remotion 会选择其他端口，以终端输出为准。

### 14.2 网页预览能做什么

- 选择 Composition。
- 播放、暂停和拖动时间线。
- 查看指定帧。
- 观察源码保存后的热更新结果。
- 从 Studio 发起渲染。

### 14.3 网页预览不能做什么

当前工程不是可视化剪辑器，不能像 Premiere/剪映一样在网页中拖动任意元素并自动保存设计。

实际编辑仍通过 `.tsx` / `.ts` 源码完成。仅拖动时间线不会修改源码；如果使用代码编辑功能修改并保存，工作区会产生 Git 差异，渲染前应执行 `git diff` 确认。

---

## 15. 正式修改流程

### 15.1 修改前

1. 阅读本交接文档。
2. 运行 `git status --short`，记录已有本地改动。
3. 播放当前最终版，确认修改对象和时间点。
4. 不覆盖 `原子游创宣传片-最终版.mp4`。

### 15.2 修改中

1. 优先修改对应场景文件，不做无关全局重构。
2. 修改镜头时长时同步调整 `timeline.ts`。
3. 时间线改变后逐条检查 `audio.tsx`。
4. 新产品页面优先使用真实截图或录屏。
5. 不使用 `Date.now()`、`Math.random()` 等非确定性动画。

### 15.3 类型检查

```powershell
npx tsc --noEmit
```

### 15.4 单帧渲染

```powershell
npx remotion still src/index.ts AtomGamePromo "out/review-next/frame-390.png" --frame=390
```

单帧应放在 `out/review*` 或 `out/qa/`，不要提交到 Git。

### 15.5 候选全片渲染

正式渲染使用 CPU `libx264`：

```powershell
npx remotion render src/index.ts AtomGamePromo "out/原子游创宣传片-候选-v4.mp4" --codec=h264 --crf=18 --concurrency=4 --log=info
```

约定：

- 正式候选使用 CPU，不使用 NVENC 作为最终交付方案。
- 不直接覆盖最终版。
- 每轮使用新的候选文件名。
- 通过验收后再改名为新的最终版。

### 15.6 局部帧范围渲染

只检查局部修改时可使用：

```powershell
npx remotion render src/index.ts AtomGamePromo "out/review-next/segment.mp4" --frames=2615-2940 --codec=h264 --crf=18 --concurrency=4
```

局部通过后仍需至少完成一次全片渲染，检查场景接缝和音频。

---

## 16. FFmpeg 验收命令

以下命令在 `promo-video` 目录运行。

### 16.1 编码信息

```powershell
ffprobe -v error -show_entries format=duration,size,bit_rate:stream=index,codec_name,codec_type,width,height,r_frame_rate,pix_fmt,channels,sample_rate -of json "out/原子游创宣传片-候选-v4.mp4"
```

### 16.2 黑场检测

```powershell
ffmpeg -hide_banner -i "out/原子游创宣传片-候选-v4.mp4" -vf "blackdetect=d=0.10:pix_th=0.10" -an -f null NUL
```

### 16.3 冻结段检测

```powershell
ffmpeg -hide_banner -i "out/原子游创宣传片-候选-v4.mp4" -vf "freezedetect=n=-60dB:d=0.75" -an -f null NUL
```

### 16.4 音量检测

```powershell
ffmpeg -hide_banner -i "out/原子游创宣传片-候选-v4.mp4" -af volumedetect -vn -f null NUL
```

### 16.5 抽取关键帧

```powershell
ffmpeg -hide_banner -loglevel error -i "out/原子游创宣传片-候选-v4.mp4" -vf "select='eq(n\,2628)+eq(n\,2631)+eq(n\,2634)',scale=640:360,tile=3x1" -frames:v 1 -y "out/review-next/ecosystem-transition.png"
```

### 16.6 建议重点检查的边界

- 16.83s：Dora SSR Logo → 多语言
- 26.63s：编码智能体 → Dora 真实工作区
- 38.43s：Dora 结果 → DSGO
- 48.27s：发现 → 详情
- 52.20s：详情 → 游玩
- 60.07s：手机 → 掌机
- 78.77s：社区 → 赛事
- 87.70s：瀑布墙 → 奖项
- 90.70s 左右：奖项 → 承办/协办单位
- 93.50s 左右：承办/协办单位 → 合作伙伴
- 97.53s：生态 → 品牌收束
- 104.53s → 0s：循环接缝

---

## 17. 不可回退的制作决策

后续修改不得无意中恢复以下旧问题：

- 不把 Dora SSR 移回 DSGO 之后。
- 不在 Dora 录屏顶部恢复白条、标签栏或地址栏。
- 不恢复 DSGO 左上角多余线条。
- 不恢复详情段额外箭头。
- 不删掌机段，除非重新确认叙事策略。
- 不把赛事三卡改回普通平铺。
- 不改变瀑布墙三列方向、周期和透视参数。
- 不把瀑布墙恢复为过长展示。
- 不重复使用“让好的游戏走得更远”等同义文案。
- 不在生态三个阶段之间使用会造成同坐标叠字的交叉淡化。
- 不把合作机构改回白底截图。
- 不让 AtomGit 名称留在图片内部造成排版不一致。
- 不用旧版 DSGO 示例视频替换现有新录屏。
- 不直接用 GPU 版本替换 CPU 正式渲染。

---

## 18. 仍待解决的风险与建议

### P1：公开发布前必须处理

1. **音频授权**：主片使用的 `pop.mp3` 来源待考，公开或商业播放前需要替换或补齐授权。
2. **录屏隐私**：逐帧检查 Dora 工作区目录、文件名、手机账号、头像、通知和测试数据。
3. **合作机构许可**：确认三家机构 Logo 和名称允许用于公开宣传。
4. **GitHub 公开内容**：确认商业计划书、原始录屏和素材是否允许公开。

### P2：展厅传播效果

1. **远距离可读性**：整体偏暗，小字较多，应在真实展厅屏幕上测试。
2. **缺少行动入口**：结尾没有二维码、网址或体验地址。
3. **时长较长**：104.5 秒适合完整介绍，但路过观众可能无法看完。
4. **首个强产品结果出现较晚**：可考虑制作 2-3 秒结果冷开场的展厅短版。
5. **循环接缝**：当前是可接受的章节重置，不是真正无缝循环。

### P3：平台分发

当前最终版是展厅版，不建议原样作为唯一 Bilibili 内容。Bilibili 版建议：

- 75-90 秒。
- 前 3 秒直接展示“一句话 → 可玩的游戏”。
- 增加旁白或更大的移动端字幕。
- 明确写出“Dora SSR 驱动创作，DSGO 连接发现、游玩与社区”。
- 结尾增加二维码、官网、GitHub 或体验入口。
- 使用高质量 H.264、BT.709、1080p30 上传母版，给平台二次压缩保留空间。

推荐标题方向：

```text
一句话生成可玩的游戏？我们做了原子游创｜Dora SSR × DSGO
```

---

## 19. 运营与内容延展建议

宣传片只是建立认知，后续运营应围绕以下内容持续产出：

- Dora SSR 从需求到游戏结果的真实教程。
- DSGO 每周优秀游戏推荐。
- 创作者采访和开发故事。
- 主题创作挑战与 Game Jam。
- 赛事作品试玩合集。
- 版本更新与真实产品进展。

核心增长闭环：

`吸引创作者 → 使用 Dora SSR 创作 → 发布到 DSGO → 玩家游玩和反馈 → 作品迭代/参赛 → 吸引更多创作者`

运营指标不要只看播放量，应关注：首次生成成功率、创作完成率、发布率、游戏游玩量、评论率和创作者留存。

---

## 20. 故障排查

### 20.1 素材为空或无法播放

- 先检查 Git LFS 是否拉取完成。
- 检查 `public/media/` 中文件大小是否正常，不应只有一百多字节。
- 用 `ffprobe` 检查素材编码。

### 20.2 Remotion Studio 打不开

- 确认当前目录是 `promo-video`。
- 运行 `npm install`。
- 检查端口是否被占用。
- 删除 `.remotion/` 或缓存后重新启动，但不要删除素材。

### 20.3 中文乱码

PowerShell 读取文件时显式使用：

```powershell
Get-Content -LiteralPath '.\项目交接说明.md' -Raw -Encoding UTF8
```

### 20.4 字体或换行变化

- 确认 Windows 已安装 Microsoft YaHei UI、Microsoft YaHei 和 Consolas。
- 不要在未截图验收的 Linux 环境直接产出最终版。

### 20.5 画面有白条或裁切错误

- 不要只改 `objectFit` 猜测。
- 用原视频尺寸和目标窗口尺寸计算裁切。
- 抽取成片而不是源码预览的帧进行验证。

### 20.6 时间线修改后声音错位

- 检查 `SHOTS` 起点是否改变。
- 检查所有 SFX 是否使用相对表达式。
- 渲染相关局部片段并逐条试听。

---

## 21. 最终交付检查表

### 内容与逻辑

- [ ] 原子游创、Dora SSR、DSGO 三者关系准确。
- [ ] Dora SSR 在多语言和编码能力之前明确出现。
- [ ] Dora 创作结果自然过渡到 DSGO。
- [ ] DSGO 同时展示发现、游玩、创作和社区。
- [ ] 赛事、奖项、机构和伙伴顺序正确。
- [ ] 文案没有重复、误导或夸大。

### 画面

- [ ] 1920x1080、30fps、16:9。
- [ ] 无顶部白条、左上多余线条和额外箭头。
- [ ] 手机、掌机、Dora 工作区没有关键内容裁切。
- [ ] 文字不越界、不重叠。
- [ ] 生态三阶段无双曝光。
- [ ] 结尾 Logo 完整并停留足够时间。
- [ ] 在真实展厅屏幕和距离下测试亮度与字号。

### 声音

- [ ] AAC 48kHz 双声道存在。
- [ ] BGM 和 SFX 无削波。
- [ ] 转场音效与画面关键帧对齐。
- [ ] `pop.mp3` 已替换或授权已确认。
- [ ] 展厅实际音量不过大、不扰民。

### 合规

- [ ] Dora 与手机录屏无敏感信息。
- [ ] 合作机构 Logo 已获许可。
- [ ] BGM、SFX 和游戏素材授权已复核。
- [ ] GitHub 公开仓库内容已经过负责人确认。

### 工程

- [ ] `npx tsc --noEmit` 通过。
- [ ] `git diff --check` 通过。
- [ ] 候选全片完成黑场、冻结段、响度和关键帧检查。
- [ ] 最终文件保留 SHA-256 记录。
- [ ] QA 文件未进入 Git。
- [ ] 最终 MP4 未覆盖历史文件，命名清楚。

---

## 22. 建议的下一步优先级

1. 确认公开 GitHub 中的商业计划书和原始素材是否允许公开。
2. 替换或确认 `pop.mp3` 授权。
3. 逐帧完成 Dora 工作区和手机录屏的隐私检查。
4. 确认合作机构 Logo 的公开展示许可。
5. 确定展厅是否需要二维码、官网或体验入口。
6. 在真实展厅屏幕上测试亮度、字号和观看距离。
7. 决定是否制作 55-65 秒展厅短版。
8. 制作独立的 75-90 秒 Bilibili 版本，而不是直接上传当前展厅版。

---

## 23. 新对话建议开场提示

```text
请先完整阅读 E:\原子游创宣传片\项目交接说明.md。
当前主成片是 E:\原子游创宣传片\promo-video\out\原子游创宣传片-最终版.mp4，
正式 Composition 是 AtomGamePromo，总长 3136 帧、30fps。
修改前先运行 git status，保留已有本地改动；不要恢复交接文档中标记为不可回退的旧实现。
修改后先做 TypeScript 检查、局部成片抽帧和全片 CPU 渲染，再提交候选版本。
```

---

## 24. 交接结论

当前版本已经解决了主要产品顺序、素材裁切、DSGO 新录屏、Dora SSR 能力展示、赛事瀑布墙、生态叠字和合作机构排版问题，具备作为内部确认版和展厅候选终版的技术基础。

后续工作的重点不应继续无目的增加动画，而应转向：

- 公开发布授权与隐私合规。
- 展厅远距离可读性。
- 行动入口和转化设计。
- 针对 Bilibili 等平台重新剪辑传播版本。

任何后续修改都必须先维护正确的产品逻辑，再考虑视觉效果。
